{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block stylesheets %}
    {{ encore_entry_link_tags('booking') }}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('booking') }}
{% endblock %}

{% block title %}Edit Hotel{% endblock %}

{% block body %}
<div class="card bg-transparent mx-auto border-0" style="max-width: 400px;" >

<h1>{{'Edit Hotel' | trans}}</h1>

    {{ include('hotel/_form.html.twig', {'button_label': 'Update' | trans }) }}

    <div class="d-flex gap-2 mt-2">
        <a class="btn btn-warning" href="{{ path('app_hotel_index') }}">{{ 'Back to list' | trans }}</a>
        {{ include('hotel/_delete_form.html.twig') }}
    </div>


    </div>
{% endblock %}
